import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  scene: {
  },

  text: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#FFFFFF',
  },
});
