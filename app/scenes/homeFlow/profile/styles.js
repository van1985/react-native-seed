import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  scene: {
    flex: 1,
    flexDirection: 'column',
  },

  text: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
