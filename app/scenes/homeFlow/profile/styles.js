import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  scene: {
    flex: 1,
    justifyContent: 'center'
  },

  text: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center'
  }
});